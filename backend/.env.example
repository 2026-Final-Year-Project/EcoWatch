PORT=5050
CLIENT_ORIGIN=http://localhost:3000
MODEL_API_URL=http://localhost:8000
# Default: backend/data/ecowatch.sqlite, independent of working directory.
# DATABASE_PATH=/absolute/path/ecowatch.sqlite
# First initialization only. Defaults to false in production, true otherwise.
# SEED_DEMO_DATA=false
# Optional source directory for the one-time legacy JSON import.
# LEGACY_DATA_DIR=/absolute/path/legacy-data
