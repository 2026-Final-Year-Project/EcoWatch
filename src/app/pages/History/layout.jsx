export const metadata = {
  title: "History | EcoWatch",
  description: "Real-time environmental monitoring through satellite imagery and AI.",
};

export default function HistoryLayout({ children }) {
  return <>{children}</>;
}